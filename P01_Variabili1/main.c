#include <stdio.h>
#include <stdlib.h>

int main()
{
  int n;
  n = 12;

  printf("valore di n: %d \n", n);

  int x,y,z;
  x = 5;
  y = 10;
  z = -1;

  int somma = x + y + z;
  // + - * /
  printf("somma = %d \n", somma);

  printf("coord: (%d, %d, %d) \n", x, y, z);

  return 0;
}
